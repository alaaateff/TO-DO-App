kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: jenkins-pvc-file-system
spec:
  volumeMode: Filesystem
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: storageclass-efs