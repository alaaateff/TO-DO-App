apiVersion: apps/v1
kind: Deployment
metadata:
  name: react-deployment
spec:
  template:
    spec:
      containers:
      - name: react
        resources:
          requests:
            cpu: "300m"
            memory: "256Mi"