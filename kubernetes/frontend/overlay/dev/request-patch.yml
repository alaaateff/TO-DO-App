apiVersion: apps/v1
kind: Deployment
metadata:
  name: react-deployment
spec:
  template:
    spec:
      containers:
      - name: react
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
            