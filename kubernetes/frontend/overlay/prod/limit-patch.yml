apiVersion: apps/v1
kind: Deployment
metadata:
  name: react-deployment
spec:
  template:
    spec:
      containers:
      - name: react
        resources:
          limits:
            cpu: "1"
            memory: "512Mi"